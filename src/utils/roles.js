const ROLES = {
  SUPER_ADMIN: 'super_admin',
  COMPETITION_ADMIN: 'competition_admin'
};

const MANAGEMENT_ROLES = [ROLES.SUPER_ADMIN, ROLES.COMPETITION_ADMIN];

module.exports = {
  ROLES,
  MANAGEMENT_ROLES
};
